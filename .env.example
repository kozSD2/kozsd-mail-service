# Database Configuration
DATABASE_URL="postgresql://kozsd_admin:secure_password_123@localhost:5432/kozsd_mail"
REDIS_URL="redis://localhost:6379"

# JWT Secrets (Generate secure keys in production)
JWT_ACCESS_SECRET="kozsd_jwt_access_secret_change_in_production"
JWT_REFRESH_SECRET="kozsd_jwt_refresh_secret_change_in_production"

# AES Encryption (32-byte key for AES-256)
AES_ENCRYPTION_KEY="kozsd_aes_256_encryption_key_change_in_production"

# AWS Services (For production)
AWS_ACCESS_KEY_ID=""
AWS_SECRET_ACCESS_KEY=""
AWS_S3_BUCKET="kozsd-mail-attachments"
AWS_SES_REGION="us-east-1"

# Email Configuration
SMTP_HOST="localhost"
SMTP_PORT=587
SMTP_USER=""
SMTP_PASS=""

# Rate Limiting Configuration
RATE_LIMIT_WINDOW_MS=3600000
RATE_LIMIT_MAX_REQUESTS=50
RATE_LIMIT_MAX_REQUESTS_EDU=200

# Application Configuration
NODE_ENV=development
PORT=3001
FRONTEND_URL="http://localhost:3000"
ADMIN_URL="http://localhost:3002"

# Security Configuration
BCRYPT_SALT_ROUNDS=12
SESSION_SECRET="kozsd_session_secret_change_in_production"

# File Upload Configuration
MAX_FILE_SIZE=25000000
ALLOWED_FILE_TYPES="jpg,jpeg,png,gif,pdf,doc,docx,txt,zip"

# Logging Configuration
LOG_LEVEL=info
LOG_FORMAT=json

# MinIO Configuration (Local S3 alternative)
MINIO_ENDPOINT="localhost:9000"
MINIO_ACCESS_KEY="kozsd_minio"
MINIO_SECRET_KEY="secure_minio_123"
MINIO_BUCKET="kozsd-attachments"